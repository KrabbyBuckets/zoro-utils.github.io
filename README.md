Example
=
```js
/*import Packages For Declarance*/
const DiscordJS = require('discord.js')
const ZoroUtils = require('zoro-utils')
const client = new DiscordJS.Client()

new ZoroUtils(client /* pass in the client*/ {
    options
})

client.login(token)
```

